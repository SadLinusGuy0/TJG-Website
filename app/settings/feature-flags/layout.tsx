import { routeMetadata } from '../../../lib/routeMetadata';
export function generateMetadata() { return routeMetadata('/settings/feature-flags', 'Feature flags', 'Feature flags for That Josh Guy.', true); }
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
